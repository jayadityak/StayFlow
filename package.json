{
  "name": "stayflow",
  "version": "1.0.0",
  "private": true,
  "description": "QR-based in-stay guest request and ordering system for hotels",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "db:migrate": "cd server && npx prisma migrate dev",
    "db:seed": "cd server && npm run db:seed",
    "db:studio": "cd server && npm run db:studio",
    "db:reset": "cd server && npx prisma migrate reset && npm run db:seed",
    "db:reset-demo": "cd server && npx tsx prisma/reset-demo.ts"
  }
}
